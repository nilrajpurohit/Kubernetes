apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: nginx-daemonset
  labels:
    layer: backend
    version: alpine
spec:
  selector:
    matchLabels:
      layer: backend
    matchExpressions:
    - {key: version, operator: In, values: [alpine]}
  template:
    metadata:
      labels:
        layer: backend
        version: alpine
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80